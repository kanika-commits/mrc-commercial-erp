"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import PlatformShell from "../../PlatformShell";
import { supabase } from "@/lib/supabase";
import { normalizeTenantSlug, tenantSlugError } from "@/lib/platformTenantSlug";

const MODULES = ["dashboard", "administration", "hr", "labour", "purchase", "store", "commercial", "accounts", "reports"];
const NAMES: Record<string, string> = { dashboard: "Dashboard", administration: "Administration", hr: "Human Resources", labour: "Labour", purchase: "Procurement", store: "Store", commercial: "Commercial", accounts: "Accounts", reports: "Reports" };

const STEPS = ["Organization", "Modules", "Default Roles", "Primary Administrator", "Domain", "Review"];
const initial = { name: "", code: "", company: "", companyCode: "", slug: "", modules: ["dashboard", "administration", "hr"], roleTemplateIds: [] as string[], adminName: "", adminEmail: "" };
function suggestSlug(value: string) { return value.toLowerCase().replace(/\.[a-z]+\.?$/i, "").replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 63); }

export default function NewOrganizationPage() {
  const [step, setStep] = useState(0); const [templates, setTemplates] = useState<any[]>([]); const [form, setForm] = useState(initial); const [slugEdited, setSlugEdited] = useState(false); const [error, setError] = useState(""); const [slugAvailability, setSlugAvailability] = useState<any>(null);
  useEffect(() => { void (async () => { const { data: { session } } = await supabase.auth.getSession(); const response = await fetch("/api/platform/role-templates", { headers: { Authorization: `Bearer ${session?.access_token || ""}` } }); if (response.ok) setTemplates((await response.json()).templates || []); })(); }, []);
  useEffect(() => { const slug = normalizeTenantSlug(form.slug); if (!slug || tenantSlugError(slug)) { setSlugAvailability(null); return; } const timer = window.setTimeout(async () => { const { data: { session } } = await supabase.auth.getSession(); const response = await fetch(`/api/platform/organizations/subdomain-availability?slug=${encodeURIComponent(slug)}`, { headers: { Authorization: `Bearer ${session?.access_token || ""}` } }); setSlugAvailability(response.ok ? await response.json() : null); }, 300); return () => window.clearTimeout(timer); }, [form.slug]);
  const compatible = useMemo(() => templates.filter((row) => row.status === "active" && !row.is_tenant_admin_template && (row.permissions || []).every((permission: any) => form.modules.includes(permission.module_code))), [templates, form.modules]);
  const selectedNames = form.roleTemplateIds.map((id) => templates.find((row) => row.id === id)?.name).filter(Boolean).join(", ");
  const update = (key: string, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const updateOrganizationName = (value: string) => setForm((current) => ({ ...current, name: value, slug: slugEdited ? current.slug : suggestSlug(value) }));
  const toggleModule = (module: string) => { if (["dashboard", "administration"].includes(module)) return; setForm((current) => ({ ...current, modules: current.modules.includes(module) ? current.modules.filter((value) => value !== module) : [...current.modules, module] })); };
  const toggleTemplate = (id: string) => setForm((current) => ({ ...current, roleTemplateIds: current.roleTemplateIds.includes(id) ? current.roleTemplateIds.filter((value) => value !== id) : [...current.roleTemplateIds, id] }));
  const validateStep = () => {
    if (step === 0 && [form.name, form.code, form.company, form.companyCode, form.slug].some((value) => !value.trim())) return "Complete the required organization, company, and SiteQube URL fields before continuing.";
    if (step === 3 && (!form.adminName.trim() || !/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(form.adminEmail.trim()))) return "Enter a valid Primary Administrator name and email.";
    if (step === 4 && (tenantSlugError(form.slug) || slugAvailability?.available === false)) return slugAvailability?.reason === "already_taken" ? "This SiteQube URL is already in use." : slugAvailability?.reason === "reserved" ? "This SiteQube URL is reserved." : "Use lowercase letters, numbers and internal hyphens only.";
    return "";
  };
  const next = () => { const message = validateStep(); if (message) { setError(message); return; } setError(""); setStep((value) => value + 1); };
  return <PlatformShell><div className="mx-auto max-w-5xl"><Link href="/platform/organizations" className="text-sm font-semibold text-slate-600">← Organizations</Link><header className="mb-8 mt-6"><p className="text-sm font-semibold text-slate-500">SITEQUBE PLATFORM / ONBOARDING</p><h1 className="mt-2 text-3xl font-semibold">Add Organization</h1><p className="mt-2 text-slate-500">Prepare a managed tenant. The Platform Owner remains outside the tenant; provisioning remains disabled for review.</p></header><div className="mb-8 grid grid-cols-3 gap-2 text-xs font-medium text-slate-500 sm:grid-cols-6">{STEPS.map((label, index) => <div key={label}><div className={`h-1 rounded ${index <= step ? "bg-slate-950" : "bg-slate-200"}`} /><span className="mt-2 block">{index + 1}. {label}</span></div>)}</div>{error && <div role="alert" className="mb-4 rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<section className="rounded-xl border bg-white p-5 sm:p-8">{step === 0 && <Step title="Organization details"><div className="grid gap-4 sm:grid-cols-2"><Field label="Organization name" required value={form.name} onChange={updateOrganizationName} /><Field label="Organization code" required value={form.code} onChange={(value) => update("code", value)} /><Field label="Initial company" required value={form.company} onChange={(value) => update("company", value)} /><Field label="Company code" required value={form.companyCode} onChange={(value) => update("companyCode", value)} /><label className="block text-sm font-medium sm:col-span-2">SiteQube URL *<div className="mt-1 flex items-center gap-2"><input aria-label="SiteQube URL" value={form.slug} onChange={(event) => { setSlugEdited(true); update("slug", event.target.value.toLowerCase().replace(/[^a-z0-9-]/g, "")); }} className="h-10 min-w-0 flex-1 rounded-md border px-3" /><span className="text-sm text-slate-500">.siteqube.com</span></div><span className="mt-1 block text-xs font-normal text-slate-500">Your organization will use this address to access SiteQube.</span>{slugAvailability?.available && <span className="mt-1 block text-xs font-semibold text-emerald-700">✓ Available</span>}{slugAvailability?.available === false && <span className="mt-1 block text-xs font-semibold text-red-700">{slugAvailability.reason === "already_taken" ? "This SiteQube URL is already in use." : "This SiteQube URL is unavailable."}</span>}</label></div></Step>}{step === 1 && <Step title="Choose modules"><p className="mb-4 text-sm text-slate-500">Core modules are required by the SiteQube shell. Business modules are selected for this tenant.</p><p className="mb-2 text-xs font-semibold uppercase tracking-wide text-slate-500">Core modules</p><div className="grid gap-3 sm:grid-cols-2">{MODULES.map((module) => <label key={module} className="flex items-center gap-3 rounded-lg border p-4"><input type="checkbox" checked={form.modules.includes(module)} disabled={["dashboard", "administration"].includes(module)} onChange={() => toggleModule(module)} />{NAMES[module]}{["dashboard", "administration"].includes(module) && <span className="text-xs text-slate-400">Required</span>}</label>)}</div><p className="mb-2 mt-6 text-xs font-semibold uppercase tracking-wide text-slate-500">Business modules</p><p className="text-sm text-slate-500">Human Resources includes Employee Registration and Employee Attendance.</p></Step>}{step === 2 && <Step title="Default roles"><p className="mb-4 text-sm text-slate-500">Select reusable active roles compatible with the enabled modules. Tenant Administrator is created automatically on invitation acceptance.</p><div className="grid gap-3">{compatible.map((template) => <label key={template.id} className="flex items-start gap-3 rounded-lg border p-4"><input type="checkbox" checked={form.roleTemplateIds.includes(template.id)} onChange={() => toggleTemplate(template.id)} /><span><strong className="block">{template.name}</strong><span className="text-sm text-slate-500">{template.description || "No description"} · {(template.permissions || []).length} permissions</span></span></label>)}{!compatible.length && <p className="text-sm text-slate-500">No active templates are compatible with the selected modules.</p>}<p className="text-xs text-slate-500">Inactive templates and templates requiring unentitled modules are unavailable.</p></div></Step>}{step === 3 && <Step title="Primary Administrator"><p className="mb-4 text-sm text-slate-500">This person will become the Tenant Administrator and receive an invitation after the organization is created.</p><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name" required value={form.adminName} onChange={(value) => update("adminName", value)} /><Field label="Email" required type="email" value={form.adminEmail} onChange={(value) => update("adminEmail", value)} /></div></Step>}{step === 4 && <Step title="Domain"><p className="mb-4 text-sm text-slate-500">Choose the SiteQube URL for this organization.</p><Summary label="SiteQube URL" value={`${form.slug || "your-tenant"}.siteqube.com`} /><p className="mt-3 text-xs text-slate-500">Your organization will access SiteQube using this URL.</p></Step>}{step === 5 && <Step title="Review"><div className="space-y-3 text-sm"><Summary label="Organization name" value={form.name || "Not entered"} /><Summary label="Organization code" value={form.code || "Not entered"} /><Summary label="Initial company" value={form.company || "Not entered"} /><Summary label="Company code" value={form.companyCode || "Not entered"} /><Summary label="Core modules" value="Dashboard, Administration" /><Summary label="Business modules" value={form.modules.filter((module) => !["dashboard", "administration"].includes(module)).map((module) => NAMES[module]).join(", ") || "None"} /><Summary label="Selected default roles" value={selectedNames || "None"} /><Summary label="Tenant Administrator" value="Created automatically on invitation acceptance" /><Summary label="Primary Administrator" value={form.adminName || "Not entered"} /><Summary label="Administrator email" value={form.adminEmail || "Not entered"} /><Summary label="Invitation" value="Sent after the organization is created" /><Summary label="SiteQube URL" value={`${form.slug || "your-tenant"}.siteqube.com`} /></div><div className="mt-6 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Provisioning is intentionally disabled. No organization, user, invitation, or DNS record will be created.</div></Step>}</section><div className="mt-4 flex justify-between"><button type="button" disabled={step === 0} onClick={() => { setError(""); setStep((value) => value - 1); }} className="rounded-md border px-4 py-2 text-sm disabled:opacity-40">Back</button>{step < STEPS.length - 1 ? <button type="button" onClick={next} className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Continue</button> : <button type="button" disabled title="Provisioning remains disabled" className="rounded-md bg-slate-300 px-4 py-2 text-sm font-semibold text-slate-600">Provision Organization</button>}</div></div></PlatformShell>;
}
function Step({ title, children }: { title: string; children: React.ReactNode }) { return <div><h2 className="text-xl font-semibold">{title}</h2><div className="mt-6">{children}</div></div>; }
function Field({ label, value, onChange, type = "text", required = false }: { label: string; value: string; onChange: (value: string) => void; type?: string; required?: boolean }) { return <label className="block text-sm font-medium">{label}{required ? " *" : ""}<input type={type} value={value} onChange={(event) => onChange(event.target.value)} className="mt-1 h-10 w-full rounded-md border px-3" /></label>; }
function Summary({ label, value }: { label: string; value: string }) { return <div className="flex flex-col gap-1 rounded-lg border p-3 sm:flex-row sm:justify-between"><span className="text-slate-500">{label}</span><strong className="sm:text-right">{value}</strong></div>; }
