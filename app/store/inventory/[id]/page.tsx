"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useEffect, useState } from "react"; import { ArrowLeft } from "lucide-react"; import { apiFetch } from "@/components/hr/hrClient";
export default function InventoryDetailPage() { const { id } = useParams<{ id: string }>(); const [data, setData] = useState<any>(); const [message, setMessage] = useState(""); useEffect(() => { apiFetch(`/api/procurement/inventory/${id}`).then(setData).catch((e: any) => setMessage(e.message)); }, [id]); return <section className="space-y-6"><Link href="/store/inventory" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-600"><ArrowLeft className="h-4 w-4" />Back to Inventory</Link>{message && <p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{message}</p>}{data && <><header><p className="text-xs font-semibold uppercase text-emerald-700">Inventory Detail</p><h1 className="mt-1 text-3xl font-bold">{data.balance.item_name_snapshot}</h1><p className="text-sm text-slate-500">{data.balance.uom_snapshot} · On hand {data.balance.quantity_on_hand}</p></header><section className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Reference</th><th className="px-4 py-3 text-right">In</th><th className="px-4 py-3 text-right">Out</th><th className="px-4 py-3 text-right">Running Balance</th></tr></thead><tbody className="divide-y">{(data.movements || []).map((row: any) => <tr key={row.id}><td className="px-4 py-3">{row.movement_date}</td><td className="px-4 py-3">{row.reference_label}</td><td className="px-4 py-3 text-right">{row.quantity_in}</td><td className="px-4 py-3 text-right">{row.quantity_out}</td><td className="px-4 py-3 text-right font-semibold">{row.running_balance}</td></tr>)}</tbody></table></section></>}</section>; }
